class ExampleModel {
  static async getData() {
    // Método placeholder: substitua por sua lógica real de acesso a banco de dados ou outra fonte
    return { message: "Dados de exemplo" };
  }
}

module.exports = ExampleModel; 